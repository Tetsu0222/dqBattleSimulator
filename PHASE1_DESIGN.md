# Phase 1 設計書 — 戦闘ロジックの安全網としてのテスト導入

## ゴール

Phase 2 以降のリファクタで「動作が変わっていないこと」を担保するためのテストを整備する。
**テストの目的は仕様の明文化** であり、現状のコードが正しいことを証明するものではない(バグも含めて現仕様)。

---

## 全体方針

### 1. 「現状の挙動を固定する」スタンス

- **現状のコードのバグを直さない**。バグも含めて「Phase 1 時点の仕様」としてテストに記録する。
- バグを見つけたら `// TODO: Phase 2 で要検討` というコメントだけ残してテストはそのまま通す。
- これにより Phase 2 以降に「これはバグ修正なのか、リファクタによる退行なのか」を区別できる。

### 2. リファクタ容易性で対象を選ぶ(網羅性ではなく)

戦闘ロジック全体をスコープとしつつ、「テストしやすいもの」から書く。テストしにくいコードは Phase 2 で構造を変えてからテストする。

**テストしやすさの判定軸:**

- ✅ **入出力が明確** — 引数とフィールドだけで結果が決まる
- ✅ **副作用が少ない** — DB アクセス・ファイル IO がない
- ⚠️ **乱数を使う** — テスト可能だが工夫が必要(後述)
- ❌ **状態を持つクラス全体に依存** — Phase 2 の構造変更を待つ

### 3. 乱数の扱い

`new Random()` を直接呼んでいる箇所が多数ある。Phase 1 では以下の戦略:

- **戦略A**: `Random` をフィールドに持たせて、テストでモック注入
- **戦略B**: 境界値テスト(damage が必ず正/必ず 0 になる引数)で乱数の影響を排除
- **戦略C**: 同じシードで複数回実行して期待値の範囲をテスト

**推奨は B → A**。B で書ける範囲を先に書き、足りない部分だけ A に進む。A は本格的にやると Phase 2 のスコープに食い込むので、Phase 1 では最小限。

### 4. テストの命名と構造

JUnit 5 を使う(build.gradle に既に依存あり)。
- テストクラス名: `<対象クラス名>Test`
- テストメソッド名: 日本語可。`damage_防御中の敵には半減する()` のようにケースを明示
- AAA パターン(Arrange / Act / Assert)を意識する

---

## 推奨スコープと優先順位

戦闘ロジック全体は広いので、以下の順で進めることを推奨する。各ステップで「テストが書ける構造になっているか」を判断し、書けないなら Phase 2 に回す。

### 優先度 高(Phase 1 でやる)

1. **`action.Attack`(通常攻撃)** — 入口として最重要
   - ダメージ計算: `(攻撃力 - 防御力/2) + 乱数`
   - 会心: `攻撃力 * 2 + 乱数`(発生率 250/255 ≒ 98%、※コメント上は逆だが実装は閾値 `> 6`)
   - 防御中の半減
   - ダメージ < 0 のときの 0 補正
   - HP < 0 のときの撃破処理(`Funeral.execution` に委譲)
   - 睡眠解除

2. **`process.IsDefense`** — 防御判定の純粋関数(おそらく)。Attack のテストで間接的に使うので先に固める

3. **`process.Awakening`** — 睡眠解除の純粋関数(おそらく)。同上

4. **`status.*`(状態異常クラス群)** — `Sleep`, `Poison`, `Burn`, `Paralysis`, `Confusion`, `Swoon`, `Dead`, `Normal`, `HolyBarrier`, `Defense`, `MagicBarrier`, `Hubaha`
   - `equals` / `hashCode` の挙動(Set 比較で使われている)
   - 各状態のターン経過処理があれば、その仕様

### 優先度 中

5. **`action.magic.MagicAttack` と `action.skill.SkillAttack`** — 攻撃魔法/特技のダメージ計算
6. **`action.magic.RecoveryMagic` と `action.skill.RecoverySkill`** — 回復ロジック
7. **`process.Funeral`** — 死亡処理の状態遷移
8. **`process.ConsumptionMP`** — MP 消費の境界値(0 のとき、足りないとき)

### 優先度 低(Phase 2 で構造を変えてから)

- **`battle.Battle` と `battle.support.BattleSupport*`** — 状態を多数持つので Phase 1 では避ける。Phase 2 で純粋関数を抽出してからテスト
- **`controller.*`** — 統合テスト相当。`@SpringBootTest` が必要で重いので後回し
- **`action.startskill.*` と `action.endskill.*`** — 戦闘フロー全体に依存

---

## テスト追加の進め方(1ステップずつ)

各ステップは「テストを1〜数件書いて → 緑 → コミット」を1サイクルとする。コミット粒度は小さく。

### ステップ 1: 環境確認(15分)

- `./gradlew test` が通ることを確認(既存の `Rpg3ApplicationTests` だけのはず)
- IDE で JUnit が走ることを確認

### ステップ 2: 最も簡単な純粋関数 1 つ(30分)

- 候補: `IsDefense.isDefense()` / `status.Sleep` の `equals`
- 「テスト1個 → 緑 → コミット」のサイクルを体感する
- 設計書に書いてない違和感(命名ミス・型のおかしさ)を見つけたらメモするだけで直さない

### ステップ 3: `Attack` クラスの非乱数部分(60分)

- ダメージ < 0 → 0 補正のテスト
- HP < 0 → 撃破のテスト
- 防御中の半減(乱数でブレるが、極端な値で境界が見える)

### ステップ 4: `Attack` クラスの乱数部分(60分)

- ここで戦略 A(Random 注入)が要るか、B(境界値)で足りるか判断
- 判断は実際に書いてみて決める。先に決めない

### ステップ 5: 状態異常の仕様固定(時間次第)

- 各 `Status` クラスについて「いま何ができるか」をテストで記述
- 多くは `equals` ベースの判定だけかもしれない(=テストも軽い)

### ステップ 6: 攻撃魔法/特技/回復(時間次第)

- ステップ 3〜4 で得た知見を流用
- 計算式が `Attack` と似ているなら、テストヘルパーの抽出も検討

### ステップ 7: Phase 1 の終わり方を決める

- 「網羅率 X% 達成」ではなく「主要パスは固まった」と感じたら次フェーズへ
- 残った領域は Phase 2 のリファクタ前提箇所として記録

---

## 判断が必要な分岐ポイント(進めながら相談すること)

以下に当たったら手を止めて Claude に相談してください:

- **乱数の扱いを A/B どちらにするか迷ったとき** — 実コードを見て判断
- **テストを書いていて「このクラスは中身を変えないとテストできない」と感じたとき** — Phase 2 に回すか即時リファクタするかの判断
- **既存コードのバグらしき挙動を見つけたとき** — Phase 1 のスタンス上は固定するが、メモは残したい
- **テストヘルパー(共通の Mock や Builder)を作りたくなったとき** — 早すぎる抽象化を避けるため、3 回繰り返してから

---

## 完了条件(ゆるく)

- 主要な攻撃ロジック(通常/魔法/特技)のダメージ計算がテストで固定されている
- 状態異常クラスの基本仕様がテストで明文化されている
- `./gradlew test` が緑で、CI 化の足場ができている
- 「Phase 2 のリファクタ対象」と「Phase 1 でカバーできなかった領域」のメモが残っている

ここまで来たら Phase 1.5 (Docker 化) へ。

---

## Phase 1.5 予告 — Docker 化(Claude が担当)

Phase 1 完了後、ポートフォリオの「誰でも触れる」性を確保するために以下を Claude が作成する:

- `Dockerfile` — Spring Boot アプリのコンテナ化(マルチステージビルド)
- `docker-compose.yml` — app + MySQL の 2 コンテナ構成
- `application.properties` の環境変数化 — 接続情報を `${DB_HOST}` 等に
- `.dockerignore` — ビルド成果物等の除外
- README に `docker compose up` の手順を追記

**完了条件**: `git clone` → `docker compose up` だけでアプリが起動し、ブラウザで動作確認できる。
**スコープ外**: K8s, Helm, 本番デプロイ用イメージの最適化, multi-arch ビルド。

完了後、Phase 1 のテストが Docker 環境でも緑であることを確認して Phase 2 へ。
