# やることリスト
## No1 ビジネスロジックをサービスクラスへ寄せる
- 魔法と特技のコントローラーのビジネスロジックをサービスクラスへ寄せる。

## No2 サービスとDTOに分離
残課題（次回への申し送り）
Funeral のインスタンス／static 混在の整理 — 今回は構造そのまま移動。設計の分離はリファクタリングの別タスクとして残してあるわね。

## No3 ビジネスロジックをサービスクラスへ寄せる
- バトルコントローラーのビジネスロジックをサービスクラスへ寄せる。

## No4 turnqueueの整理
BattleProgressService.turnAction の引数 turnqueue も将来的には battleState.getTurnQueue() 経由で取り出せるようになったので、引数から外す余地が出てきました（今回のスコープ外ですが、もし気になるようでしたら別タスクで）。

## No5 参照渡しによる状態変更
後で俯瞰して見てから対応。
コントローラーファイルとサービスクラスで、参照渡しで処理している。
⇒リファクタリングの視点で見ると、最小単位で変更となると妥当だが、将来的には直したい。
